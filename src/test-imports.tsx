import { FormMessage } from "@/components/form-message";
import { SubmitButton } from "@/components/submit-button";

// This is just a test file to verify imports work
console.log(FormMessage, SubmitButton);
